class Solution {
  public:
    void reverseArray(vector<int> &arr) {
        int n=arr.size();
        int st=0;
        int end=n-1;
        // code here
         while(st<=end)
        {
           swap(arr[st],arr[end]);
           st++;
           end--;
        }
    }
};
