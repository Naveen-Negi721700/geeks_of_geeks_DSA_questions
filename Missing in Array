class Solution {
  public:
    int missingNum(vector<int>& arr) {
        // code here
        int n=arr.size()+1;
        long long total=1LL*n*(n+1)/2;
        long long ans=0;
        for(int x:arr)
        {
            ans+=x;
        }
        return total-ans;
    }
};
